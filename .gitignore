# Re-downloaded series (reproduction output). data/baseline/ and data/full/ are
# deliverables and stay tracked.
data/downloaded/

# Exported session transcripts, never tracked
sessions/

# Python
__pycache__/
*.pyc
.venv/
venv/
env/
.ipynb_checkpoints/

# Credentials (none are needed by this study; belt and braces)
.env
.env.*
*.key
*.pem
credentials*
secrets*
*.token

# Scratch
results/*.tmp
